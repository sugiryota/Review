<h1><%= t("フォロー中") %></h1>

<table>
  <% @followings.each do |user| %>
    <tr>
      <td>
        <% if user.image.attached? %>
          <%= image_tag user.image.variant(resize_to_limit: [30, 30]) %>
        <% end %>
      </td>
      <td><%= link_to user.nickname, user_path(user) %></td>
      
    </tr>
  <% end %>
</table>

<%= link_to t("button.back"), user_path %>